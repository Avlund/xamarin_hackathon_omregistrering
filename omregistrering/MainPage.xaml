<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="omregistrering.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:omregistrering">
    <StackLayout>
        <Label
            BackgroundColor="#cccccc"
            FontSize="25"
            HorizontalOptions="FillAndExpand"
            HorizontalTextAlignment="Center"
            Text="Omregistrering"
            VerticalOptions="Center" />
        <StackLayout>
            <Label
                FontSize="15"
                HorizontalOptions="Center"
                Text="Anvend som:"
                VerticalOptions="Center" />
            <Button
                Clicked="OnBuyerButtonClicked"
                Text="Køber"
                VerticalOptions="Center" />
            <Button
                Clicked="OnSellerButtonClicked"
                Text="Sælger"
                VerticalOptions="Center" />
        </StackLayout>
    </StackLayout>
</ContentPage>