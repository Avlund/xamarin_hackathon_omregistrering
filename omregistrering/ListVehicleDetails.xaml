<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="omregistrering.ListVehicleDetails"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    NavigationPage.HasBackButton="False">
    <ContentPage.Content>
        <StackLayout Padding="10,5,0,5" BackgroundColor="#cccccc">
            <Label
                    FontSize="30"
                    HorizontalOptions="Center"
                    Text="Køretøjs detaljer"
                    VerticalOptions="Center" />

            <Label x:Name="makeLabel"
               Text="{Binding VehicleInstance.RegNumber}"
               FontSize="20"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
        </StackLayout>

        <!--ItemsSource="{Binding vehicle}"-->
        
        <!--<ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label
                        FontSize="20"
                        HorizontalOptions="Center"
                        Text="{Binding RegNumber}" 
                        VerticalTextAlignment="Center" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>-->        
    </ContentPage.Content>
</ContentPage>